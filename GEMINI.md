# 全局行为规范：先出方案（Plan-First），审阅批准后方可执行

## 核心原则 (Core Directive)
每次接收到用户的任何开发、修改、重构或操作需求时，**严禁直接修改代码或直接执行破坏性/修改性命令**。必须严格遵循“先出 Plan、用户审阅、授权后再执行”的流程：

---

### 第一步：研究与生成方案 (Research & Generate Plan)
1. 仅使用只读工具（如 `view_file`、`list_dir`、`grep_search` 等）充分了解代码与需求背景。
2. 为用户输出清晰完整的 **实施方案（Implementation Plan）**，明确列出：
   - **目标与改动概述**：要解决什么问题、核心设计思路。
   - **涉及文件清单**：拟新增、修改或删除的具体文件及代码位置。
   - **改动细节与影响面**：具体的函数、变量、UI 及架构影响。
   - **验证计划**：如何验证功能的正确性与稳定性。
3. 若属于复杂任务或新功能开发，同步创建或更新 `implementation_plan.md`。

---

### 第二步：停止并等待用户审阅 (STOP & Wait for Approval)
1. 方案输出完毕后，**必须立即停下（STOP）**，主动请求用户审阅该 Plan。
2. **在获得用户明确同意（如回复“同意”、“确认”、“Proceed”、“开始吧”等）之前，绝对不得做任何实际的源码修改或执行写操作**。

---

### 第三步：授权后按步执行与验证 (Execute Only After Approval)
1. 获得用户审阅通过后，方可严格按照已确认的 Plan 进行分步编码与操作。
2. 执行完毕后进行严格语法与逻辑验证，并向用户汇报执行结果。